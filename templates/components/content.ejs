<% let Path = rootPath
             + "/"
             + "index.html";
             Path = Path.split(' ').join('%20');
%>
<div> <a href=<%= Path %>>
        Acceuil</a> --> Math -->
    <% for(const theme of allContent.themes) { 
        if (theme.id == currentLesson.theme) {
        %>
    <%= theme.title %>
    <% } 
        } %>
</div>

<h1><b>Leçon:</b> <%= currentLesson.title %></h1>
<% if (lessonContent.questions.length > 0) {
let questionPath = rootPath
+ "/"
+ currentLesson.theme 
+ "/"
+ currentLesson.title.replace(':', '').replace('/', '') 
+ "/question1.html";
questionPath = questionPath.split(' ').join('%20'); %>
%>
<div><a href=<%= questionPath %>> Accès aux questions</a></div>
<% } %>
<div class="container">
    <div id="myCarousel" class="carousel slide" data-interval="false">
        <!-- Wrapper for slides -->
        <div class="carousel-inner">
            <% let first = true;
                for(const page of lessonContent.pages) { 
                    const pageHtml = "../../" + page; 
                    if (first == true) {
                        first = false; %>
            <div class="item active" style="text-align: center;">
                <%- include(pageHtml) %>
            </div>
            <% } else { %>
            <div class="item" style="text-align: center;">
                <%- include(pageHtml) %>
            </div>
            <% }
        } %>
        </div>

        <!-- Left and right controls -->
        <a class="left carousel-control" href="#myCarousel" data-slide="prev">
            <span class="glyphicon glyphicon-chevron-left"></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="right carousel-control" href="#myCarousel" data-slide="next">
            <span class="glyphicon glyphicon-chevron-right"></span>
            <span class="sr-only">Next</span>
        </a>
    </div>
</div>